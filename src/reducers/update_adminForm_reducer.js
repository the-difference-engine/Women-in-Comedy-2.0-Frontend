import { updatePublicFigure } from '../actions/types';
import { updateIsMentor } from '../actions/types';

const INITIAL_STATE = {}

export default (state = INITIAL_STATE, action) => {
  /*switch(action.type) {
    case updatePublicFigure:
      return action.payload.data; 

    case updateIsMentor:
      return action.payload.data;

    default:
      return state;
  }*/
};
